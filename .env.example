# Replicate API Token (required for backend)
REPLICATE_API_TOKEN=your_replicate_api_token_here

# Backend URL (optional, defaults to /api/generate)
# Pour développement local avec Deno:
VITE_BACKEND_URL=http://localhost:8000/api/generate
# Pour production Vercel, laisser vide ou utiliser:
# VITE_BACKEND_URL=/api/generate
